# Hiragana Study 固有ルール

## 幼児向け教材の意味対応

- `KanaEntry`の`spokenLabel`・`illustrationKey`・実画像は、3歳児が見て同じ対象だと判断できる意味・種類・成長段階で一致させる。画像の寸法・容量・余白検査だけで意味一致を合格にしない。
- ひらがなの絵を追加・差し替えた場合は、個別画像を原寸で開き、46件contact sheetでも隣接教材との混同がないことを確認する。動物は種類と成長段階を明記して確認する（例: 成鳥のあひると、ひよこを区別する）。
- 通常文字の絵付き導入・形合わせは、画面表示と`AudioGuide`の両方で「`${spokenLabel}の ${character}`」を使う。音合わせは画像を正解根拠にせず、利用可能な音声と文字選択だけで構成する。頭文字ではない`を`・`ん`は`kanaAssociationLabel`の特殊用法を使い、頭文字として偽装しない。
- 完了前に、代表文字の実画面テストで表示文言・画像alt・読み上げ要求を同時に確認し、全46文字の関連付けはコンテンツテストで監査する。
- 絵の名称が複数成立する問題（例: `いぬ`・`こいぬ`・`わんちゃん`）を、画像だけで一意に答えさせない。音声が必須の問題は、音声OFF・`visual-only`・unlock後の利用不可で停滞せず、未達成のまま安全にskipできる回帰テストを持つ。
- 長押し操作は必要時間を常時表示し、押下直後から進捗を見せ、短押し・領域外移動・取消・context menuで発火しないことをテストする。操作アイコンには用途が通じる標準記号を使い、説明のないマスコット画像を操作記号へ流用しない。
- ブラウザAPIが非対応または失敗した時に無反応となる操作を常設しない。主要な学習画面には、現在の進捗を失わず庭へ戻れる明確な標準記号を置き、戻る前後で同じ文字・段階を再開できることをテストする。
- Reducerが次の文字・段階へ進む操作は、App統合テストで実画面の文字・段階も同じ結果へ進むことを確認する。初回や成功後に、説明のない単独アイコン画面や自動ホーム帰還を挟まず、ブラウザAPIの準備は意味のある教材操作へ統合して失敗時も学習を止めない。
